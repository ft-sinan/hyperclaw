{
  "name": "@hyperclaw/memory-lancedb",
  "version": "0.1.0",
  "private": true,
  "description": "Vector DB memory for HyperClaw — semantic search over conversation history",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "vectordb": "^0.21.2"
  },
  "peerDependencies": {
    "openai": "^4.0.0"
  },
  "peerDependenciesMeta": {
    "openai": {
      "optional": true
    }
  },
  "optionalDependencies": {
    "openai": "^4.0.0"
  }
}
